const user=require('../models/user.models.js')
const blog=require('../models/blog.models.js')

const subscribe_blog=(req,res)=>{
    // const view_username_id=req.user._id
    res.json({
        "message":"doing something..."
    })
}
module.exports={
    subscribe_blog
}